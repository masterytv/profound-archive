/**
 * Twitter card image for /uap/video-explore.
 * Re-exports the OG image — same content, different meta tag.
 */
export { default, alt, size, contentType, runtime, revalidate } from './opengraph-image';
