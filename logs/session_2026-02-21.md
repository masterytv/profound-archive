# Session Log: 2026-02-21 (Sprint Wrap-up)

## Summary of Recent Deployments
1. **Logo & Favicon Update**: Safely replaced `/public` assets and updated instances including `site-header.tsx`.
2. **Global UI Restyling**:
    - **Admin Intake Page**: Restyled to light slate theme, Crimson Pro headings, branded card patterns, replaced emoji with Lucide icons.
    - **Journey Flow Component**: Improved readability with solid pill-shaped nodes and gradient timelines.
3. **Directory Expansions**:
    - **Resources Page**: Added Academic Institutions, Org/Nonprofits, Books, and Support categories.
    - **Channels Page**: Fixed sorting capabilities so "High to Low" subscriber sort functions correctly. Removed `@NearDeathExperience-US` from results (marked `not_nde`).
4. **Search Experience Overhaul**:
    - **Home to Search Merging**: Replaced the previous hero CTA with a unified "Search Bar Gateway" that directs straight to the primary search interface.
    - **Renaming**: "Semantic Search" is now referred to user-facing as "Concept Search" to be more intuitive, while retaining the internal `semantic` API type.
    - **Score Filtering UI & API**: Implemented numeric sliding filters in the sidebar for Greyson Scale (0-32), Transformation Score (0-50), and Veridical (0-25). 
    - **Filter Sync & Sidebar**: Fixed scroll-locking on the filter sidebar panel and extracted the `isNde` explicit option out of the standard selection since we only index valid NDEs for normal views anyway.
    - **Schema & RPC Upgrade**: Modified Typesense schema via `index-data.ts` to sync stats, and updated `search_punctuated_embeddings_filtered` RPC in Supabase to parse min score constraint fields directly via Postgres vector search parameters.
    - **Concept Search Fix**: Resolved a 500 API error caused by an overloaded Postgres function conflict and data-type mismatches (`bigint` vs `integer`) in the original RPC implementation.

### Phase 6: Deployment Pipeline Fixes
- **Firebase App Hosting Secrets**: The Typesense API key rotation initially broke the deployment build because `apphosting.yaml` hardcoded `versions/1`.
    - Recreated the secret natively via the Firebase CLI to establish a fresh `versions/2`.
    - Updated `apphosting.yaml` to point to `versions/latest` to cleanly auto-resolve future key rotations.
    - Manually granted the `SecretAccessor` IAM binding to the `profound-archive` backend to satisfy Cloud Build permission requirements.
